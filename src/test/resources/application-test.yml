spring:
  r2dbc:
    url: r2dbc:h2:mem:///testdb;DB_CLOSE_DELAY=-1;MODE=MySQL
    username: sa
    password:
  sql:
    init:
      mode: always
logging:
  level:
    com:
      example:
        music: debug
    org:
      springframework:
        r2dbc: debug